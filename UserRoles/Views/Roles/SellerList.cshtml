@model UserRoles.Models.ViewModels.UserListViewModel
@{
    ViewData["Title"] = "SellerList";
}

<div>
    <h2 class="h2-header inline-header">Seller List</h2>
    <a href="@Url.Action("Register", "Account")" class="add-user"><i class="fas fa-plus-square fa-2x"></i></a>

    <div id="Sellers">
        @Html.Partial("_PartialTable", new ViewDataDictionary(this.ViewData) { { "type", "Seller" } })
    </div>
</div>






